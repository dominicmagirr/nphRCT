#' @param formula Formula object. The response (on the left of the `~` operator) must be a survival object as returned by the `Surv` function.
#' The terms (on the right of the `~` operator) must include the treatment arm indicator, and additionally can include strata using the `strata` function.
